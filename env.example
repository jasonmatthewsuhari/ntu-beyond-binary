# Fluent Desktop Agent Environment Variables
# Copy this file to .env and fill in your actual values

# AI Provider Selection
# Choose which AI service to use for the desktop agent
# Options: "gemini" or "openai"
AI_PROVIDER=gemini

# Google Gemini API Key (Required if AI_PROVIDER=gemini)
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key_here

# OpenAI API Key (Required if AI_PROVIDER=openai)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Desktop Agent Configuration

# Maximum number of steps before aborting (default: 50)
# AGENT_MAX_STEPS=50

# Delay in seconds after each action (default: 1.5)
# AGENT_ACTION_DELAY=1.5

# Grid spacing for coordinate overlay in pixels (default: 100)
# AGENT_GRID_SPACING=100

# Save screenshots for debugging (default: false)
# AGENT_SAVE_SCREENSHOTS=false

# Gemini model to use (default: gemini-2.0-flash-exp)
# Options: gemini-2.0-flash-exp, gemini-2.5-flash, gemini-1.5-pro
# GEMINI_MODEL=gemini-2.0-flash-exp

# OpenAI model to use (default: gpt-4-vision-preview)
# Options: gpt-4-vision-preview, gpt-4o
# OPENAI_MODEL=gpt-4-vision-preview
